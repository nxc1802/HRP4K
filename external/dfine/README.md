# D-FINE external runner

Use the official `Peterande/D-FINE` environment with `num_classes: 1`. Keep architecture internals unchanged; adapt dataset paths, schedule, checkpoint path and canonical output conversion only.
