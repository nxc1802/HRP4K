# AutoFocus reproduction

This method requires a trained category-agnostic focus head and paper-faithful FocusPixel-to-FocusChip grouping. A heuristic crop implementation must not be labelled AutoFocus. Export source-coordinate canonical detections and focus-chip metadata.
