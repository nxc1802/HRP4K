# ZoomDet reproduction

Adapt the official ZoomDet code when available. Preserve the learned offset predictor, non-uniform warp and corner-aligned box transform. Compare against Resize using the same detector family/checkpoint protocol.
