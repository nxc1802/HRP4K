# RT-DETRv1/v2 external runner

Use the official `lyuwenyu/RT-DETR` environment. Freeze separate v1 and v2 configs with one HRP4K class, train only when explicitly authorized, and export one canonical prediction document plus per-image synchronized latency metadata. Do not spawn a subprocess per image for latency measurements.
